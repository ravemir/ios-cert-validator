{
  "name": "ios-cert-validate",
  "version": "0.0.1",
  "description": "Validates a given iOS Code Sign Certificate, ensuring it has not expired or been revoked. Requires a working OpenSSL installation.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "openssl",
    "iOS",
    "ocsp"
  ],
  "author": "ravemir",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.15.1"
  }
}
